<div class="row">
    <div class="container">
    <div class="col-sm-6 col-md-6">
        <h3>{{employee.First_Name}} {{employee.Last_Name}}</h3>
        <span>{{employee.Gender}}</span>
        <p>Info</p>
        <div>
            <p>Adress</p>
            {{employee.City}}
            {{employee.Address}}
            {{employee.Code}}
        </div>
      </div>
    </div>
</div>